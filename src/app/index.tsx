import { View, Text } from "react-native"

export default function Index() {
    return(
        <View className="flex-1 items-center justify-center">
            <Text className="text-zinc-400">Convide seus amios e planeje sua prima viagem</Text>
        </View>
    )
}